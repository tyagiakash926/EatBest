-const j = `${review.rating}`
.col-sm-6.mystyle
  .testimonial-wrap
    .image-plus-name
      img(src=`${review.userImage}` alt='')
    .testimonial
      p
        | #{review.message}
    cite.testimonial-author
      span.name
        strong(style='color: black;') #{review.userName}
        |                       #{review.bookedOn}
      span.stars
        - for (var i = 0; i < j ; ++i) {
          i.fa.fa-star
        - }
